import { RechercheForfaitPipe } from './recherche-forfait.pipe';

describe('RechercheForfaitPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheForfaitPipe();
    expect(pipe).toBeTruthy();
  });
});
